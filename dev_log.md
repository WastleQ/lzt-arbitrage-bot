# Журнал разработки (dev_log.md)

## [2026-09-04] Система предупреждений и безопасность
* В `src/engine/arbitrage.py` добавлен сбор предупреждений (warnings) для каждой сделки:
  - Низкий рейтинг продавца (LZT Trust).
  - Молодой аккаунт (account_age_days).
  - Наличие исключенных слов в названии (откат, бан и т.д.).
* В `src/bot/handlers.py` карточки сделок теперь отображают предупреждения пользователю для ручного принятия решения.
* В `src/config.py` добавлены настройки `min_seller_trust`, `min_account_age_days` и `exclude_words`.
* Написаны модульные тесты для проверки всех механик предупреждений.
